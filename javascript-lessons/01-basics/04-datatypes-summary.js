/**
 * Data types: 
 * primitive datatypes and non-primitive datatypes
 * 
 * primitive datatype can be called by value;
 * non-primitive/reference datatype we can call by references;
 * 
 * 7 types: strings, numbers, booleans, null, undefined, symbol, bigInt;
 * 
 * reference(non-primitive): array, objects, functions;
 * 
 * Javascript is dynamically type language;
 */

// heap and stack memory:

/**
 * we have two types of memory or datatypes:
 * stack memory and heap memory
 * primitive(stack memory) and non-primitive(heap memory)
 * 
 */