// array push pop

// array shift unshift

/**
 * push and pop method is faster than shift and unshift;
 * push, pop: adds and removes the items from the backside;
 * unshift, shift: adds and removes the items from the frontside;
 */

let fruits = ["apple", "mango", "graphes", "oranges"];
console.log(fruits);

// push: To push the item to the array;
// fruits.push("banana");
// console.log(fruits);

// pop: To remove the item from the array from the back;
// let removedFruit = fruits.pop();
// console.log(`the removed fruit is ${removedFruit}`);

// unshift: To push the item in the front of the array;

// fruits.unshift("watermelon");
// console.log(fruits);

// shift: To remove the item from the front of the array
 
// let removedFruit2 = fruits.shift();
// console.log(fruits);
// console.log(`removed fruit is ${removedFruit2}`);