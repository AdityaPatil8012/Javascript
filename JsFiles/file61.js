// iterables;

const items = ['item1', 'item2', 'item3', 'item4'];

for(let item of items){
  console.log(item);
};

// String and array are iterables;
/**
 * array like object;
 * which has length property and 
 * When we can access those using index; 
 */